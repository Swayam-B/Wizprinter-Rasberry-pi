"""
WizPrinter Kiosk Application for Raspberry Pi.

`__version__` is the single source of truth for the app version — the Settings
footer and the update checker (wizprinter/updates.py) both read it, so bump it
here (and nowhere else) when cutting a release.
"""
__version__ = '2.5.0'

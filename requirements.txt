# WizPrinter Kiosk — Python dependencies
# Install: pip install -r requirements.txt

# Core UI
kivy>=2.3.0

# Image processing
Pillow>=10.0.0

# HTTP / API
requests>=2.31.0

# Env-file loading
python-dotenv>=1.0.0

# PDF rasterisation (pinned for ABI stability — update intentionally)
pymupdf==1.25.3

# CUPS bindings — pinned to a stable release tag, not the rolling default branch
# Run `git+https://github.com/OpenPrinting/pycups.git@2.0.4` if PyPI lags behind
pycups==2.0.4

# TTS (accessibility) — optional; installed automatically on Pi by setup_pi.sh
# pyttsx3>=2.90
